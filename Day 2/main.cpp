#include <fstream>

void read_file(const char[] &filename) {
    
}



int how_many_safe(std::ifstream file) {
    for (file) {
        is_increasing();
        is_decreasing();
    }

}


int main(int argc, char *argv[]) {
    input = read_file(argv[1]);
    how_many_safe(input);
}
